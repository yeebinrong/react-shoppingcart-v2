import { 
    Button,
    Toolbar,
    AppBar,
    IconButton,
    Typography,
} from '@material-ui/core';


export {
    Button,
    Toolbar,
    AppBar,
    IconButton,
    Typography,
}

export function MatStyle(theme) {
    return ({
        root: {
            flexGrow: 1,
        },
        title: {
            flexGrow: 1,
        },
    })
}